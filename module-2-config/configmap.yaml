apiVersion: v1
kind: ConfigMap
metadata:
  name: app-config
  labels:
    app: web-app
    module: config
data:
  APP_ENV: "production"
  LOG_LEVEL: "info"
  MAX_CONNECTIONS: "100"
  APP_VERSION: "1.0.0"
